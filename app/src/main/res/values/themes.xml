<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme — Activity uses a Compose theme, this handles the window background. -->
    <style name="Theme.FootballGroups" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
